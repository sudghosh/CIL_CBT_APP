version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: cildb
      ***REMOVED***: cildb123
      POSTGRES_DB: cil_cbt_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - cil-network
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - ***REMOVED***=postgresql://cildb:cildb123@postgres:5432/cil_cbt_db
      - ***REMOVED***=dev-secret-key-here
      - ALGORITHM=HS256
      - ***REMOVED***_EXPIRE_MINUTES=30
      - ***REMOVED***=35588221822-439neljrhq8chpqipjsavor0rtc4ni11.apps.googleusercontent.com
      - ***REMOVED***=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
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
    networks:
      - cil-network
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 8000

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=development
      - REACT_APP_API_URL=http://localhost:8000
      - REACT_APP_***REMOVED***=35588221822-439neljrhq8chpqipjsavor0rtc4ni11.apps.googleusercontent.com
      - WATCHPACK_POLLING=true
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - cil-network
    command: npm start

volumes:
  pgdata:

networks:
  cil-network:
    driver: bridge
